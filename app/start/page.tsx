import StartClient from './client'

export const metadata = {
  title: 'Start building — Zebi',
  description: 'Tell Zebi what you want to build and get a company, project, tasks and kickoff note in seconds.',
}

export default function StartPage() {
  return <StartClient />
}
